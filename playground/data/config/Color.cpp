#include"Color.h"
const vec3 Color::BLACK = vec3(0,0,0);
const vec3 Color::WHITE = vec3(1, 1, 1);
const vec3 Color::RED = vec3(1, 0, 0);
const vec3 Color::GREEN = vec3(0, 1, 0);
const vec3 Color::BLUE = vec3(0, 0, 1);